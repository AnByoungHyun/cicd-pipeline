pipeline {
    agent {
        docker {
            image 'nginx:latest'
            args '-p 8000:8000'
        }
    }
    triggers {
        githubPush()
    }
    stages {
        stage('docker tcp connect & test') {
            steps {
                sh 'echo testing...'
            }
        }
    }
}